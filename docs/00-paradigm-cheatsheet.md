# Paradigma — Cheatsheet operativo

> Documento corto, pensado para abrirse antes de cada feature.
> Para la referencia larga, ver `docs/02-architecture-principles.md`.

**Paradigma:** Hexagonal ligero + Use Cases + Single Source of Truth en PHP.
**JS proyecta. AI consume. Nadie duplica reglas.**

## Capas y dirección de dependencias (estricta, unidireccional)

```
http (AJAX)  →  application (Use Cases)  →  domain (reglas puras)  →  repositories (SQL)
                                                                 ↘  infrastructure (WP, backend adapters)
ui (JS)      →  http (vía AJAX)
```

- `domain/` no conoce `$wpdb`, `get_option`, `error_log`, `add_action` ni nada de WordPress.
- `repositories/` solo contiene SQL. Cero `if` de negocio.
- `application/` orquesta. No define reglas nuevas.
- `http/` parsea, autentica, delega y serializa. Nada más.
- `infrastructure/` integra con WP, schema, Node backend y notificaciones. El LLM se consume solo vía backend.
- `ui/` (JS) pinta y consume endpoints. No decide nada vinculante.

## Invariantes (no negociables)

1. **Una regla de negocio = un único lugar en PHP.** Si aparece dos veces, es bug.
2. **JS no decide** ocupación, colisión, precio ni estado. Solo pinta y pre-valida.
3. **Cada flujo end-to-end = 1 Use Case PHP** (`VerboCosaUseCase` con un único `execute()`).
   El controlador AJAX y el handler de AI llaman al MISMO Use Case.
4. **Models (futuros Repositories) = SQL puro.** Si un método empieza a "saber" del negocio, se mueve a un domain service.
5. **AI nunca inventa dominio.** Si necesita una regla nueva, primero se crea el service de dominio; luego el evaluator lo llama.

## Decisión rápida — "¿dónde escribo esto?"

| Si lo que voy a escribir es...                | Va en...                                |
| --------------------------------------------- | --------------------------------------- |
| Una query SQL                                 | `includes/repositories/`                |
| Una regla pura del negocio                    | `includes/domain/{contexto}/`           |
| Un flujo que orquesta varias reglas           | `includes/application/{contexto}/{Verbo}UseCase.php` |
| El handler de un request HTTP/AJAX            | `includes/http/ajax/`                   |
| Llamada a WP, Node backend o notifs           | `includes/infrastructure/{adaptador}/`  |
| Pintar DOM, calendarios, modales              | `assets/js/ui/`                         |
| Cliente HTTP que consume endpoint y cachea    | `assets/js/services/`                   |

### Contextos actuales relevantes

> **Canon DEO — fuente autoritativa:** `docs/04-canonical-constitution.md` (persistencia canónica universal). Lo que sigue bajo `canonical` es el inventario operativo de la implementación actual, incluidos elementos particulares de Finance legacy; no define por sí mismo el contrato universal ni el Shell Canónico Base v1. El plan temporal activo —incluida la ruta PCU— está en `docs/plans/shell-canonical-base-v1.md`. El shell de acceso administrativo/legal (`ResolveShellAccessUseCase`) es un concepto distinto.

> **Ruta PCU (estado):** PCU-0 completada; PCU-1 completada (`c5f97ee`); PCU-2 completada (`6fcb124`, entonces `DB_VERSION=21`); **shell family-only** sube a `DB_VERSION=22`; PCU-3 completada (`d01eb81`); PCU-4 completada (`5703f84`, `CATALOG_VERSION=1`); **PCU-5A** completada (`473cf7e`); **PCU-5B** completada (`7b9ee5a`); **SB1-5B1** completada (`de3488c`) — create container productivo; **SB1-5B2** completada (`6477886`) — create record productivo; **SB1-5B3** completada (`2596cc6`) — update record productivo; **SB1-5B4** completada (`dcb6f49`) — delete record productivo; **SB1-5B5** completada (`d339826`) — update container title/details; **SB1-5B6** completada (`ccca5e6`) — delete container + FK CASCADE: endpoint `aa_delete_canonical_container`, modal confirmación separado, hard delete + CASCADE, uncertain con recarga bloqueante, CRUD universal de containers completo; sin soft delete; sin support PHP compartido en ese ciclo; cero migración/fallback; **hotfix de orden DOM** commiteado en `cea9e00` (modal de borrado renderizado antes del script síncrono del formulario de contenedor); **SB1-5C1** (higiene del transporte AJAX de mutaciones canónicas) **implementado en working tree (sin commit)** — `CanonicalShellWriteAjaxSupport` + `CanonicalShellWriteAjaxRejection` concentran identidad/acceso/enablement, construcción del write gateway y parseo de enteros positivos para los seis endpoints universales; soporte limitado al transporte AJAX (sin SQL, sin repositories, sin comandos, sin redirects, sin JSON); se conservan las dos lecturas de enablement (gate + bootstrap); contratos, códigos, mensajes, HTTP, orden de gates y redirects intactos; la API futura reutilizará Application, no este helper WordPress. **Ciclo 1 UI — «Todas las listas»** (working tree): `module=canonical_shell` sin `family` = listado agregado (`ReadCanonicalShellAllContainersUseCase` + puerto/adaptador/repo multi-familia); alcance de consulta ≠ identidad de recurso; `lists_scope=all` solo retorno; `DB_VERSION=22` / `CATALOG_VERSION=1`. **Ciclo 2A UI** (working tree): sidebar «Listas»; filtro header Todas+familias (`Filtrar listas`); default create en JS (`resolveInitialCreateFamilyKey`); sin sync live de nav tras enablement. **Ciclo 2B.1** (working tree): `available_families` (enabled+autorizadas) unifica router/Listas/header; bare module con N=1 → 302 familiar; header N=1 estático. **Ciclo 2B.2** (working tree): FAB «Nueva lista» + retirada encabezado interno redundante en contenedores. **Ciclo 3 UI records fill** (working tree): superficie blanca fill-height en registros `resolved`+`empty|resolved_page`; Detalles plegables; FAB «Nuevo registro» (`#aa-shell-open-create-record-btn`); medición iframe cromo-only en fill; sin rediseño de cards. **Ciclo 4 UI compact overlay** (working tree): registros compactos 1 columna con panel abspos + extensor antes de paginación; Escape `preventDefault`/`defaultPrevented`; preview sin compact. **Ciclo 5 UI opciones en encabezado** (working tree): ⋮ en header solo al abrir; reserva clickeable en toggle; menú `w-[12rem] max-w-full min-w-0 box-border`; medida max(panel, menú); contorno continuo solo con foco del toggle. Shell Finance/Archive → `aa_canonical_*`; módulo clásico Finanzas → `aa_finance_*`. Finance (`aa_finance_*`) y Expedientes (`aa_expediente_*`) son legado intacto durante la transición; no son el nuevo canon. La familia `archive` es persistencia canónica independiente, no un renombrado de Expedientes. Distinguir: `module=canonical` (UI Finance AJAX) ≠ `module=canonical_shell` (SSR del shell); el adaptador JS `ExpedienteRegistrosCanonicalAdapter` es un puerto UI local de Expedientes, distinto de los adaptadores canónicos PHP del shell. **Shell family-only / DB 22** activo (sin `variant_key` en `aa_canonical_*`; rollback `docs/plans/canonical-shell-db22-rollback.md`).

- `canonical`: implementación actual del contexto canónico y de la familia Finance legacy (Ciclos 1, 2A, 3A1, 3A2, 3B1, 3B2a, 3B2b, 3C1, 3C2, 3D1, 3D2, 3D3A, 3D3B, 3D4A, 3D4B, 3E1A y 3E1B y 3E2A y 3E2B; shell SB1-1…SB1-5A1; PCU-5A enablement); dominio en `includes/domain/canonical/` (`AA_Canonical_Key`, `AA_Canonical_Family_Definition`, `AA_Canonical_Registry` instanciable y sellable); aplicación en `includes/application/canonical/` (`ResolveCanonicalRouteUseCase` para validación y resolución de ruta pura sin WordPress; cadena de lectura SB1-2A/3A y de escritura neutral SB1-5A1 sin adaptador productivo; **PCU-5A** `SetCanonicalFamilyEnabledCommand` / `SetCanonicalFamilyEnabledUseCase` / `ReadCanonicalFamilyEnablementUseCase` / `CanonicalFamilyEnablementPort` / `CanonicalFamilyEnablementSnapshot` / `CanonicalFamilyEnablementResult` + excepciones `CanonicalFamilyUnknown` / `CanonicalFamilyNotProvisioned` / `CanonicalFamilyEnablementSchemaNotReady` / `CanonicalFamilyEnablementPersistenceFailed`) y en `includes/application/finance/` (`CreateFinanceContainerUseCase`, `GetFinanceContainerUseCase`, `ListFinanceContainersUseCase`, `DeleteFinanceContainerUseCase`, `UpdateFinanceContainerUseCase`, `CreateFinanceRecordUseCase`, `GetFinanceRecordUseCase`, `ListFinanceRecordsUseCase`, `DeleteFinanceRecordUseCase`, `UpdateFinanceRecordUseCase` y `FinanceUseCaseSupport` para validación UTF-8, envelopes, normalización decimal de amount hasta 17 dígitos enteros y 2 decimales, verificación contextual anti-leak, proyección agregada de `amount_total` en contenedores y paginación fija de 15); infraestructura en `includes/infrastructure/canonical/` (`AA_Canonical_Core_Bootstrap` para composición y shared instance de familias `finance` + `archive`; `AA_Canonical_Family_Provisioner` + `CanonicalFamilyProvisioningFailed` + `AA_Canonical_Family_Catalog_Lifecycle` (`CATALOG_VERSION=1`, option `aa_canonical_family_catalog_version`, `admin_init` prio 25; provisiona filas con `is_enabled=0`/`seed_version=0` sin tocar `DB_VERSION`); **PCU-5A** `AA_Canonical_Family_Enablement_Store` (SQL acotado a `aa_canonical_families`) + `AA_Canonical_Family_Enablement_Nav`; `AA_Finance_Canonical_Read_Adapter` (clase conservada; **sin** binding productivo del shell tras PCU-5B); bootstrap de lectura `AA_Canonical_Read_Binding_Bootstrap` → Relational Read solo para familias enabled; `AA_Canonical_Write_Binding_Bootstrap` (write universal; desde SB1-5C1 los **seis** endpoints universales lo consumen a través de `CanonicalShellWriteAjaxSupport::build_write_gateway()`, que crea un registry nuevo por construcción); registries con fail-on-duplicate `[duplicate_read_binding]` / `[duplicate_write_binding]`) e `includes/infrastructure/wp/` (`AA_Canonical_Shell_Url_Policy` / `AA_Canonical_Shell_Base_Url_Policy` para URLs seguras allowlisted; `AA_Canonical_Access_Policy` para autorización neutral compartida de familias canónicas; `AA_Finance_Schema` en `FinanceSchema.php` para schema físico y FK de `aa_finance_containers` y `aa_finance_records` (legado); `AA_Canonical_Schema` en `CanonicalSchema.php` para persistencia universal `aa_canonical_families` / `aa_canonical_containers` / `aa_canonical_records` con **`DB_VERSION=22`**; repositorio universal `CanonicalRelationalRepository` + excepciones `CanonicalRelationalQueryFailed` / `CanonicalRelationalAmbiguousOutcome`; adaptadores `AA_Canonical_Relational_Read_Adapter` / `AA_Canonical_Relational_Write_Adapter` en `includes/infrastructure/canonical/relational/` (**bindings productivos del shell vía bootstraps PCU-5B** cuando la familia está enabled; el módulo clásico Finanzas sigue en `aa_finance_*`); `CanonicalReadPersistenceFailed` capturado por los Use Cases read → `contract_error`); repositorios SQL puro en `includes/repositories/` (`FinanceContainerRepository` con `update` de reemplazo completo `title`/`details` sin `updated_at`, idempotencia por relectura autoritativa y código `missing_details` cuando falta la clave `details`; `FinanceRecordRepository` para CRUD básico, `update` de reemplazo completo `title`/`details`/`amount` con identidad `record_id+container_id`, variante autorizada vía contenedor padre, claves obligatorias `details`/`amount` en update (`missing_amount` si falta `amount`), vacío de amount → `null`, relectura autoritativa e idempotencia, paginación fija de 15, suma `sum_amounts_by_container`, suma agregada en lote `sum_amounts_by_container_ids` y cascada referencial); transporte HTTP/AJAX en `includes/http/ajax/FinanceAjaxSupport.php` (soporte compartido para `aa_finance_nonce`, autorización `AA_Canonical_Access_Policy`, bootstrap y mapeo HTTP fail-closed), `includes/http/ajax/FinanceContainersAjax.php` (`aa_list_finance_containers`, `aa_create_finance_container`, `aa_get_finance_container`, `aa_delete_finance_container`, `aa_update_finance_container`), `includes/http/ajax/FinanceRecordsAjax.php` (`aa_list_finance_records`, `aa_create_finance_record`, `aa_get_finance_record`, `aa_delete_finance_record`, `aa_update_finance_record` con `container_id` obligatorio, `record_id` unificado, solo `variant_key`, preservación estricta de `amount`, reemplazo completo en update y sin `nopriv`) e **`includes/http/ajax/CanonicalFamilyEnabledAjax.php`** (`wp_ajax_aa_update_canonical_family_enabled`, nonce `aa_canonical_family_enabled`, sin nopriv; respuesta con `nav` servidor), **`includes/http/ajax/CanonicalShellWriteAjaxSupport.php`** + **`includes/http/ajax/CanonicalShellWriteAjaxRejection.php`** (SB1-5C1: soporte común de transporte para los seis endpoints universales de escritura; `authorize_identity()` devuelve la definición de familia tras núcleo → ruta → Access Policy → provisioning → enablement, `build_write_gateway()` compone registry nuevo + bootstrap → `CanonicalWriteGateway`, `parse_positive_int()` centraliza el parseo de IDs; rechaza con la terna código/mensaje/HTTP y nunca emite JSON ni redirects; cargados por el loader antes de los endpoints), **`includes/http/ajax/CanonicalCreateContainerAjax.php`** (`wp_ajax_aa_create_canonical_container`, nonce `aa_create_canonical_container`, sin nopriv; composition root write → `WriteCanonicalShellContainerUseCase::create`; Access Policy por familia resuelta vía soporte), **`includes/http/ajax/CanonicalUpdateContainerAjax.php`** (`wp_ajax_aa_update_canonical_container`, nonce `aa_update_canonical_container`, sin nopriv; → `WriteCanonicalShellContainerUseCase::update`; last-write-wins title/details; redirect página 1; `container_id` JSON null), **`includes/http/ajax/CanonicalDeleteContainerAjax.php`** (`wp_ajax_aa_delete_canonical_container`, nonce `aa_delete_canonical_container`, sin nopriv; → `WriteCanonicalShellContainerUseCase::delete`; hard delete + FK CASCADE; uncertain con `redirect_url`) e **`includes/http/ajax/CanonicalCreateRecordAjax.php`** (`wp_ajax_aa_create_canonical_record`, nonce `aa_create_canonical_record`, sin nopriv; → `WriteCanonicalShellRecordUseCase::create`; redirect `build_records_url` página 1), **`includes/http/ajax/CanonicalUpdateRecordAjax.php`** (`wp_ajax_aa_update_canonical_record`, nonce `aa_update_canonical_record`, sin nopriv; → `WriteCanonicalShellRecordUseCase::update`; last-write-wins; redirect página 1) e **`includes/http/ajax/CanonicalDeleteRecordAjax.php`** (`wp_ajax_aa_delete_canonical_record`, nonce `aa_delete_canonical_record`, sin nopriv; → `WriteCanonicalShellRecordUseCase::delete`; hard delete + touch; uncertain con `redirect_url`); UI en `includes/admin/ui/modules/canonical/` (dispatcher por mapa explícito y `_fallback.php` neutral; **`module=canonical`**) con submódulo `finance/` (`finance/index.php` con configuración `window.AA_FINANCE_DATA` aislada publicando `listContainers`, `createContainer`, `listRecords`, `createRecord`, `deleteRecord`, `getRecord`, `updateRecord`, `getContainer`, `deleteContainer` y `updateContainer`; `finance-records-module.js` como controlador de lectura paginada vía `aa_list_finance_records` con amounts `string|null`, botones «Editar» y «Eliminar» por card (edit entrega solo `{recordId,containerId,sourcePage}`; delete conserva snapshot con title/amount), estados RECORDS_*, callback retrocompatible `onAuthoritativeLoadSettled` y API `AA_FinanceRecords.createController`; `finance-record-create-module.js` como controlador de creación vía `aa_create_finance_record` con amount bruto sin parseo JS, snapshot inmutable, revisión incierta con `reviewToken`/`pendingRecordReview` en orquestador, refresh autoritativo de página 1 tras éxito confirmado y API `AA_FinanceRecordCreate.createController`; `finance-record-delete-module.js` como controlador de eliminación vía `aa_delete_finance_record` con identidad compuesta `record_id+container_id`, confirmación explícita, sin delete optimista, incertidumbre resuelta con GET `aa_get_finance_record` + refresh de `sourcePage`, invalidación temprana de snapshot de contenedores, `reviewToken`/`pendingDeleteReview`, lock de mutaciones `none|create_review|delete_active|delete_review|edit_active|edit_review` y API `AA_FinanceRecordDelete.createController`; `finance-record-edit-module.js` como controlador de edición de registros vía GET autoritativo `aa_get_finance_record` antes de modal y UPDATE `aa_update_finance_record` con reemplazo completo `title`+`details`+`amount` (claves `details`/`amount` siempre presentes en FormData), amount string bruto sin parseo JS (signo ASCII `-`), separación `sourceSnapshot`/borrador/`submissionSnapshot`, sin update optimista, refresh autoritativo de `sourcePage`, revisión incierta bifásica con `pendingRecordEditReview`/`reviewToken` en orquestador (`phase`: `awaiting_get`|`awaiting_list`, pending persistente durante navegación, retry por fase sin repetir UPDATE), `missing_details`/`missing_amount` como BLOCKED_REJECTED, y API `AA_FinanceRecordEdit.createController`; `finance-container-delete-module.js` como controlador de eliminación de contenedores vía `aa_delete_finance_container` con identidad `id+variantKey`, copy de cascada FK, un solo DELETE (sin borrar registros desde JS), sin delete optimista, incertidumbre resuelta con GET `aa_get_finance_container` + refresh autoritativo del listado, `reviewToken`/`pendingContainerDeleteReview`, locks cruzados con creación de contenedor/registros (`containerMutationLock`: `none|delete_active|delete_review|edit_active|edit_review`), navegación/paginación durante revisión (pending no sobrevive reload) y API compacta `AA_FinanceContainerDelete.createController`; `finance-container-edit-module.js` como controlador de edición de contenedores vía GET autoritativo `aa_get_finance_container` antes de abrir modal y UPDATE `aa_update_finance_container` con reemplazo completo `title`+`details` (clave `details` siempre presente), separación `sourceSnapshot`/borrador/`submissionSnapshot`, sin update optimista, refresh autoritativo del listado, revisión incierta bifásica (`phase`: `awaiting_get`|`awaiting_list`, retry por fase sin repetir UPDATE), locks `edit_active`/`edit_review` cruzados con create/delete/registros y API `AA_FinanceContainerEdit.createController`; `finance-module.js` como orquestador con navegación interna CONTAINER_LIST/RECORDS_DETAIL sin cambio de URL, botón «Ver registros», botón «Editar» por card de contenedor con GET previo obligatorio, botón «Eliminar» por card de contenedor con snapshot `{containerId,title,details,amountTotal,sourcePage}`, botón «Nueva entrada» solo en detalle, preservación de snapshot del listado, degradación segura si falta factory/markup, máquina de creación de contenedores IDLE/EDITING/SUBMITTING/FIELD_REJECTED/BLOCKED_REJECTED/CONFIRMED/UNCERTAIN/REVIEWING_UNCERTAIN/DRAFT_REVIEWED, máquina de creación de registros separada, modal accesible, snapshot inmutable, timer de foco cancelable, foco a `#aa-finance-status` tras settlement/Cerrar y revisar, cierre unificado de BLOCKED_REJECTED a IDLE, focus trap con visibilidad real, prevención de doble submit y sincronización con página 1 de lectura); Settings PCU-5A: sección `#aa-canonical-record-types-root` fuera de `options.php` + `canonical-family-toggles.js` + `AA_CANONICAL_FAMILY_ENABLED` (persistencia AJAX; sin regeneración live de sidebar/nav tras Ciclo 2A); sidebar «Listas» (`build_module_url` o URL familiar si N=1, activo en `canonical_shell`); filtro header Todas+familias vía `available_families` (`aa-family-switcher` si N≥2; estático si N≤1); FAB «Nueva lista» (`#aa-shell-fab-stack` / `#aa-shell-open-create-btn`) en listados de contenedores; gate router `family_disabled` HTTP 200; layout ligero `includes/admin/ui/shared/canonical-layout.php`. Transporte Finance vía `module=canonical&family=X[&variant=Y]`. Shell SSR paralelo vía `module=canonical_shell` (compositor, preview fail-closed). Edición completa de registros (3E2B) en UI Finance; eliminación de contenedor solo desde listado.
- `booking`: creacion/resolucion de citas y disponibilidad asociada.
- `auth` (C2+C3B agenda magic-link): GET `admin-post` `aa_agenda_access` (bridge HTML, no consume) → POST `aa_agenda_access_consume` → `ConsumeAgendaAccessTokenUseCase` (1× HMAC `/agenda/access/consume`); C3B CTA `login_message` + POST `aa_agenda_access_request` → gate `AA_Agenda_Access_Request_Eligibility` (`AA_Installation_Provisioning_Detector::is_provisioned()` + `aa_client_secret` + `AA_API_BASE_URL`) → nonce → `RequestAgendaAccessLinkUseCase` (transient 60s/blog + 1× HMAC `/agenda/access/request` body vacío) → PRG neutro `aa_agenda_access_link_sent`; dominio `Token_Format` / `User_Policy` / `Redirect_Policy` / `Request_Eligibility`; infra `AA_Agenda_Access_Backend_Client` (`consume`+`request`) + `Session_Issuer`; handlers en `AgendaAccessHandlers.php`. C3A rate-limits; independientes OAuth sin marcador managed: CTA oculto y 0 HMAC.
- `legal`: acceso de shell vía `ResolveShellAccessUseCase` (free por defecto; `legal_gate`/`full` solo con `subscription_active === true` del backend HMAC); gate bloqueante reutiliza `GetLegalGateStatusUseCase` / `AcceptAgendaTermsUseCase` / `AcceptAgendaPrivacyAndTermsUseCase` + `AA_Legal_Gate_Backend_Client`; UI en `includes/admin/ui/legal-gate/` sin sidebar/módulos; flujo moderno provisionado `needs_terms` (solo Términos) y cohorte installation-anchored `needs_privacy_and_terms` (privacidad + términos atómicos); no reaceptación por versión; el transient `ready` residual no autoriza acceso. **Shell general → legal gate asíncrono y fail-open mientras no haya confirmación:** el router (`ui/index.php`) NO ejecuta `ResolveShellAccessUseCase` en la navegación normal (calendar/clients/etc. responden HTML sin esperar al backend); la resolución corre tras el render vía `assets/js/services/shellAccessProjection.js` (proyección SOLO de UX, promesa compartida, caché corta full/free aislada por `blogId+authSessionId`, `legal_gate` nunca cacheado). `access === legal_gate` dispara navegación dirigida al marcador interno `aa_gate=1` (acompañado del nonce `aa_legal_gate_nonce`), que reejecuta el resolver de forma síncrona y renderiza `legal-gate/index.php` si sigue gated, o elimina el marcador y vuelve a la URL canónica en cualquier otro caso (sin bucles tras aceptar). **Expedientes → fail-closed y autoritativo en URL/AJAX:** resolución síncrona `access === full` para `module=expedientes` y para la vista legacy `clients/expediente` (misma rama del router, antes de `layout.php`; el archivo sigue teniendo exactamente 2 `ResolveShellAccessUseCase()->execute()`: marcador `aa_gate` + este gate) y para todos los AJAX de padre/registros/adjuntos (cualquier resultado ≠ `full` ⇒ 403). Sidebar: ítem **Expedientes** (`module=expedientes`) independiente de **Clientes** (`module=clients`). El booleano UX de la vista legacy arranca `false` y solo una respuesta `full` viva lo habilita. No altera el fail-open del shell ni Clientes general. **Schema (DB_VERSION=18):** catálogo `aa_expediente_categories` (seed idempotente `slug=general` y `slug=clientes`) + entidad padre `aa_expedientes` (`category_id` NOT NULL; `client_id` nullable UNIQUE — NULL=general, N=materializado desde cliente; sin FK); registros tienen `client_id` y `expediente_id` nullable (DB 16); **adjuntos `aa_expediente_adjuntos`:** pertenencia = `record_id`; `client_id` nullable (DB 18 — snapshot/compat opcional; NULL=sin cliente; nunca 0; `ensure_expediente_adjuntos_client_id_nullable`); sin columna `expediente_id` en adjuntos; paths Storage duales en `ExpedienteAdjuntoVariants` (`client_v1` legacy `/clients/{id}/…` y `expediente_v2` `/expedientes/{id}/…`); **P2:** list/sign/delete canónico validan identidad dual vía `AA_Expediente_Adjunto_Identity_Policy` (relacionado v1/v2 + general v2); **P3:** writer canónico permanente `expediente_v2` (sin enablement/toggle/constante); orphan histórico sin `expediente_id` conserva `client_v1`; despliegue: 1) backend Node compatible 2) plugin writer v2; materialización del padre cliente **solo** en create legacy válido (`CreateExpedienteRegistroForClientUseCase`: TX MySQL get-or-create padre vía `INSERT … ON DUPLICATE KEY UPDATE id = LAST_INSERT_ID(id)` + hijo bridged `client_id+expediente_id`; categoría `clientes`; sin backfill de huérfanos `expediente_id IS NULL`); escritura canónica vía `CreateExpedienteRegistroUseCase` (padre con `client_id` ⇒ puente; padre general ⇒ `client_id` NULL; ignora owners del HTTP) expuesta por AJAX `aa_create_expediente_registro_for_expediente` y lectura AJAX paginada `aa_list_expediente_registros_for_expediente` vía `ListExpedienteRegistrosWithPublicAdjuntosUseCase` (mismo nonce `aa_expediente_registros_by_expediente_nonce`; envelope con `adjuntos`/`adjunto` = DTO público `{id,width,height,byte_size,created_at}`; policy dual P2; `ExpedienteRegistrosByExpedienteAjax`) y sign-read canónico `aa_sign_expediente_adjunto_read_for_expediente` vía `ExpedienteAdjuntosByExpedienteAjax` + `GetExpedienteAdjuntoReadUrlForExpedienteUseCase` (owner/`client_id` derivados en servidor; policy dual P2; variants `summary|gallery|display`; generales ya no `attachments_unavailable`) y attach canónico `aa_attach_expediente_adjunto_for_expediente` vía `UploadExpedienteAdjuntoForExpedienteUseCase` (**P3 writer permanente `expediente_v2`**; general → metadata `client_id` NULL; relacionado → snapshot del padre; legacy bridged con `expediente_id` delega siempre al canónico; orphan histórico sin `expediente_id` conserva `client_v1`; locks **aggregate→`storage_quota:1`** (release inverso); compensación `delete_object` si se pierde lock tras Storage salvo metadata idempotente same-op; idempotencia por `upload_operation_id`; sin caller visual; sin cambio AJAX/UI; orden de despliegue: 1) backend Node compatible 2) plugin writer v2) y delete canónico `aa_delete_expediente_adjunto_for_expediente` vía `DeleteExpedienteAdjuntoForExpedienteUseCase` (owner/`client_id` derivados en servidor; policy dual P2; Storage primero → DELETE metadata exacta; `already_absent` remoto permite completar retries; segunda eliminación tras éxito → `not_found`/404); **C1b:** factory JS `AAAdmin.ExpedienteRegistrosCanonicalAdapter.build` → `{scopeKey,capabilities,ports}` (sin `client_id`; ports `update`/`deleteRegistro` activos; inválida → `null`). **C1c1:** swap SSR→live tras `onInitialLoad`; thumbs/galería/sign/delete-adjunto; paginación SSR. **C1c2:** tras readiness+`successUrl`, FAB clonado → `ExpedienteRegistros.openCreate`; `onCreateComplete({recordId,imageOutcome:none|saved|failed|abandoned})` → una `location.assign(successUrl)`; provisional como fallback pre-swap/fallo/`successUrl` inválida; destroy restaura FAB; adjuntos/Storage anclados a `client_id` legacy. **D1:** tras create legacy terminal, host `clients-module` captura `record.id→expediente_id` del envelope (sin mutarlo), `onCreateComplete` → una `location.replace` a `detailCanonicalBaseUrl`+`expediente_id` (iframe; create legacy → canónico). **D2:** tras gate `access === full`, la ruta legacy `clients&view=expediente` con `client_id` escalar válido consulta una vez `ExpedientesRepository::find_by_client_id` (triestado: `array` padre válido / `false` sin padre / `null` SQL o fila malformada); padre válido → `wp_safe_redirect` 302 a `module=expedientes&view=detail&expediente_id` (URL construida en router solo con params aprobados; sin copiar `client_id`/`records_page`/nonce/query arbitraria); sin padre → vista virtual legacy; `null`/owner contradictorio/`wp_safe_redirect===false` → 500 fail-closed sin layout; D2 no consulta/migra/lista huérfanos históricos — el backfill de instalaciones históricas es prerrequisito operativo independiente antes de desplegar D2 en producción que deba conservarlos.  **Delete canónico:** `DeleteExpedienteRegistroForExpedienteUseCase` + `aa_delete_expediente_registro_for_expediente`; padre cliente → 1× `DeleteExpedienteRegistroUseCase`; padre general → `has_any_by_record_id` fail-closed + `delete_by_id_for_expediente`; adapter `deleteRegistro`; detail `deleteRegistro=true`; paginación stale post-delete aceptada; sin D2. **Ciclo A (coordinación de mutaciones):** named lock MySQL `AA_Expediente_Aggregate_Lock` (`includes/infrastructure/wp/class-aa-expediente-aggregate-lock.php`); scopes server-side `client:{client_id}` (expediente relacionado) y `expediente:{expediente_id}` (general); writers: create for-client, create canónico, upload canónico/legacy (locks **aggregate luego site `storage_quota:1`** durante authorize→transfer→finalize→insert; release inverso; compensación `delete_object` si se pierde el lock tras Storage), delete registro/adjunto individual legacy+canónico (una sola adquisición en wrappers); `resource_busy`/`concurrent_change` → 409; `coordination_failed`/`coordination_lost` → 500; `storage_cleanup_failed` → 502; sin cambio de schema/`DB_VERSION`. **Ciclo B (delete canónico de contenedor):** `DeleteExpedienteUseCase` + AJAX `aa_delete_expediente` (`ExpedientesAjax`, nonce `aa_expedientes_nonce`, sin nopriv); `expediente_id` = aggregate root; `client_id` = relación/contexto residual (nunca desde el request); preflight JOIN adjuntos→registros por `expediente_id`; padre general con cualquier adjunto → `aggregate_inconsistent`/409; relacionado: Storage `delete_object` (acepta `deleted`/`already_absent`) → `assert_held` → DELETE metadata exacta; registros y padre permanecen hasta TX final corta (`FOR UPDATE` + DELETE registros por `expediente_id` + DELETE padre por identidad); progreso parcial reintentable; objetos Storage sin metadata fuera de alcance; nunca elimina `aa_clientes` ni categorías; envelope `{deleted,expediente_id}`; HTTP 400/403/404/409/500/502. **Ciclo C–D (UI contenedor en detail canónico):** menú ⋮ en `#aa-expediente-detail-header` (`.aa-options-trigger-flat`); acciones «Editar» (título) y «Eliminar»; capabilities visuales `containerActions.capabilities.updateTitle/delete` (por defecto true para toda la familia; no son autorización — seguridad = manage_options+nonce+gate+UC); config aditiva `containerActions` (`updateTitleAction`/`deleteAction`/`nonce`/`listUrl`/`title`) separada del nonce/actions de registros; update `aa_update_expediente` + `UpdateExpedienteUseCase` solo `expediente_id`+`title` (reutiliza `AA_Expediente_Create_Policy`); título independiente del cliente — `get_or_create_for_client` no sobrescribe title en ON DUPLICATE; delete confirmación irreversible vía `AAAdmin.modal`; POST delete solo `expediente_id` → `location.replace(listUrl)`; script `expediente-detail-actions.js` solo en detail; sin branching por categoría; imágenes generales fuera de alcance. **Update canónico:** `UpdateExpedienteRegistroForExpedienteUseCase` + `aa_update_expediente_registro_for_expediente` + repo `update_title_body_for_expediente` (WHERE id+expediente_id); adapter port `update`; detail `updateRegistro=true` / `deleteRegistro=false`; policy Create reutilizada; sin delete registro/D2. **Listado padre:** `ListExpedientesUseCase` página interna de 15, búsqueda solo por `title`, sin techo 100; alta vía `CreateExpedienteUseCase` + `AA_Expediente_Create_Policy`. UI del módulo `expedientes`: `expedientes-module.js` consume `aa_list_expedientes` (query/page; cards `[data-aa-card]`, buscador y paginador propios). **AJAX padre** `aa_list_expedientes` / `aa_create_expediente` (`ExpedientesAjax`): `manage_options` + nonce + gate fail-closed `access === full` (reutiliza `require_expediente_shell_access`). **Detalle padre:** `module=expedientes&view=detail&expediente_id=N` se resuelve con `GetExpedienteUseCase` + `ExpedientesRepository::find_by_id` + `ListExpedienteRegistrosUseCase` **después** del gate `full` y **antes** de `layout.php` (id decimal positivo estricto; inválido → 400, inexistente → 404; fallo inesperado al listar hijos → 500; sin JS legacy, sin AJAX ni `blog_id`). La ruta legacy `clients&view=expediente` permanece como vista virtual solo sin padre; con padre redirige al detail canónico (D2).
- `onboarding`: estado de activacion inicial hacia la primera cita.
- `learning`: recomendaciones de producto; el catalogo declara intencion (`action`) y Application normaliza el payload ejecutable.
- `executable`: contrato comun de proyeccion (`AA_Executable_Contract`) para listas/items ejecutables; mappers en `application/executable/` traducen salidas de fuentes (learning, tasks) sin fusionar persistencia. Ver `docs/tasks-executable-contract.md`.
- `executive`: Propuesta ejecutiva (MC1) — `AA_Executive_Proposal_Policy` elige lista foco + top-3 desde snapshot de `GetTaskBoardUseCase`; `GetExecutiveProposalUseCase` orquesta sin feed executable ni `executive_candidates` legacy; acciones filtradas en `AA_Executive_Actions_Policy`. **MC2:** GET `aa_get_executive_proposal` + render read-only. **MC3:** POST `aa_executive_action` vía `RecordExecutiveActionUseCase` (complete/dismiss/navigate/handler); UI usa `data-executive-*` sin coordinator legacy. **MC4:** sprint ejecutivo invisible en `user_meta` (`aa_executive_sprint_v1`) vía `ExecutiveSprintStateRepository` + `AA_Executive_Sprint_Policy`; solo `RecordExecutiveActionUseCase` renueva por acción; `GetExecutiveProposalUseCase` puede actualizar foco sin renovar TTL si lista agotada durante sprint activo; complete/navigate/handler renuevan 1h; dismiss no renueva. **MC4.1:** `meta.sprint` en payload (no renderizado) + consola `AAExecutiveProposal.debugSprint()` / `debugSprintWatch()` para observabilidad local. **MC5:** foco manual separado en `user_meta` (`aa_executive_focus_state_v1`) vía `ExecutiveFocusStateRepository` + `AA_Executive_Focus_State_Policy`; selección aleatoria en `AA_Executive_Focus_Selection_Policy`; `ChangeExecutiveFocusUseCase` + POST `aa_executive_focus_action` (`change_focus`/`previous_focus`/`expire_sprint_debug`); `ExecutiveFocusTransitionService` comparte cambio aleatorio con tercer dismiss fuera de sprint; UI foco usa `data-executive-focus-action` (no `data-executive-action`); `meta.focus_controls` alimenta botones; `meta.focus_state` solo debug/tests; Cambiar foco/Anterior no inician sprint; tercer `Ahora no` fuera de sprint cambia foco aleatorio sin iniciar sprint. **Fuente comun objetivo (MC13O-A/B1):** `docs/tasks-common-source-target.md` documenta el destino y MC13O-B1 prepara schema base y MC13O-B2 sube a `DB_VERSION=7` agregando `aa_task_actions` como tabla task-only de acciones declaradas. **MC13O-C1/C2:** seed/sync manual del catalogo Learning hacia DB comun via `SyncLearningCatalogToTasksUseCase`, `SeededTaskRepository` y `TaskActionRepository`; no hay consumers ni hooks automaticos. **MC13O-D1A:** el read path de Tasks ya interpreta metadata comun (`source_category`, `origin_key`, `managed_by`, `default_bucket`) y `aa_task_actions` para seeded `agenda_app`. **MC13O-D2:** `GetExecutableListsFeedUseCase` prefiere la lista seeded DB comun (`agenda_app` + `learning.recommendations` activa con tareas en payload) y omite el mapeo Learning legacy; sin seeded lista conserva fallback legacy. **MC13O-D3:** `AA_Learning_Catalog_Seed_Lifecycle` corre sync en `admin_init` prioridad 20 controlado por `AA_Learning_Catalog::SEED_VERSION` vs `aa_learning_catalog_seed_version`; sync archived-first y activa lista solo al validar seed completo. No reemplaza migracion de schema (`AA_Schema`). **MC13O-E1:** `TaskSystemCompletionFactResolver` + `EvaluateTaskSystemCompletionFactsUseCase` evaluan `completion_fact_key` y persisten `completed_by_system` en `aa_task_state` sin mezclar con `status=done`. **MC13O-E2:** `GetTaskBoardUseCase` ejecuta el evaluator al inicio; active projection distingue `status=done` (manual) de `completed_by_system` (system); `completion_type=system` no expone completar manual en mapper. **MC13O-F1:** `MigrateLearningRecommendationStateToTaskStateUseCase` migra manual/idempotente completion manual e ignored→defer; no transportar dismissed/aging/list_override al motor comun todavia. **MC13O-F2:** `AA_Learning_State_Migration_Lifecycle` corre en `admin_init` prio 21 tras seed (`aa_learning_state_migration_version`); dismissed sigue sin migrar. **MC13O-F3:** `GetExecutableListsFeedUseCase` omite Learning legacy solo si DB seeded lista y (migración al día o sin legacy actionable); `is_dismissed` cuenta como actionable. **Labels de buckets (MC13O-0):** copy canónico `primary→Principales`, `secondary→Secundarias` vive en el contrato; los mappers no inventan labels por fuente. **Listas MVP (MC13J):** feed unified executable por defecto; única vista oficial. **MC13J-2C:** modos `user`/`user-swap` retirados; DOM user-only eliminado; `AAExecutableUserListsVisibleFeed` conserva nombre (opera sobre unified). **Procedencia de lista es metadata del contrato** (`source`, `source_category`, `source_label`); el renderer solo presenta `source_label`, no inventa copy primario (MC13M). `source` sigue siendo canal técnico; no motivo de secciones UI separadas (MC13H unified). **Señales/estado/eventos:** las acciones del usuario registran señales interpretables por policies; no son reglas absolutas. **`dismiss_*` = naming técnico heredado; producto = Ahora no (ocultar temporalmente; designorar al expirar `dismiss_until`).** **MC13O-H1/H2:** acción dismiss escribe `dismiss_until` temporal vía `AA_Task_Work_Cycle_Policy` (reset diario 12:00, default 1 ciclo); `dismiss_until=null` con historial = ocultamiento permanente legacy. **MC13O-H3A:** `can_dismiss` ya no depende de defer ni bucket; dismiss no es defer. **MC13O-H3C:** click Ahora no en agenda_app DB común usa canal Tasks (`data-tasks-action="dismiss"`). **MC13O-H3B-1:** `default_bucket` es propiedad editable vía `ChangeTaskDefaultBucketUseCase`. **MC13O-H3B-2:** backfill idempotente defer histórico→`default_bucket=secondary`; `defer_*` queda auditoría legacy. **MC13O-H3B-3:** `aa_tasks.default_bucket` es la única fuente activa de `primary`/`secondary`; `defer` / “Ahora no” queda deprecated/dormido, `can_defer=false`, `visible_actions` no emite `defer`, `state.ignored` ya no significa `has_defer`. **MC13O-H3B-close:** tests/docs alineados; feed unified no emite `defer`; runtime defer dormido aceptable pre-MVP. **MC13O-consolidation-audit:** tests confirman feed oficial unified; Learning no renderiza listas en Listas/Tareas; dedup seeded/fallback en `assemble_lists`; system vacía oculta / user vacía persiste. **Tasks active:** `AA_Task_Active_View_Projection_Policy` proyecta buckets por `default_bucket` + dismiss/system completion; `AA_Task_Prioritization_Policy` solo ordena. **Buckets:** `primary`/`secondary`/`default` son resultado de una `view`; las policies interpretan criterios, los renderers solo presentan.

### Learning: acciones primarias

- El catalogo (`includes/domain/learning/`) puede declarar `action` opcional como intencion: `navigate`, `handler` o `null`.
- `GetLearningRecommendationsUseCase` normaliza esa intencion: resuelve URLs para `navigate`, valida `handler`, y conserva `navigation` como adapter legacy.
- La UI solo renderiza/ejecuta el contrato ya normalizado; la disponibilidad runtime de handlers (ej. PWA install) vive en JS, no en PHP.
- `learning-action-handlers.js` registra handlers por clave estable. Un handler solo se renderiza si existe, tiene `run` y `isAvailable(action, item)` confirma disponibilidad.
- `pwa.install` es el primer handler real: captura `beforeinstallprompt`/`appinstalled` en closure (sin `window.deferredPrompt`) y solo está disponible si hay prompt diferido y no se está en modo standalone.

## Naming (para archivos NUEVOS, no migramos lo viejo todavía)

- **PHP clases:** `AA_{Contexto}_{Rol}` → archivo `class-aa-{contexto}-{rol}.php`
- **Use Cases:** `{Verbo}{Cosa}UseCase` → archivo `{Verbo}{Cosa}UseCase.php` (PascalCase)
- **JS:** `camelCase.js`, `export default`

Lo viejo coexiste con su nombre histórico hasta que se toque por otra razón.

## Antes de añadir código, pregúntate:

- ¿Estoy creando una segunda fuente de verdad? → si sí, **para**.
- ¿El controller/handler tiene lógica que no sea "parsear y delegar"? → extrae a Use Case.
- ¿El JS está calculando algo que el PHP debería calcular? → mueve el cálculo a PHP.
- ¿Esta regla podría correr en CLI sin WordPress? → entonces es **dominio**, no infrastructure.
- ¿Esto representa una intención del usuario formulable como "verbo + cosa"? → es un **Use Case**.

## Glosario mínimo

- **Domain:** reglas que serían ciertas aunque WordPress no existiera. Sin SQL, sin WP. Determinista y testeable.
- **Use Case (Application):** orquestador de un flujo del producto. Una clase, un método público (`execute`). No define reglas, las coordina.
- **Repository:** acceso a BD. Solo SQL. Sin `if` de negocio.
- **Controller AJAX:** traductor HTTP↔Use Case. Autentica, sanitiza, delega, serializa.
- **Infrastructure:** todo lo que toca el mundo exterior (WP, MySQL via repos, LLMs, webhooks, notifs).
- **UI:** todo lo que pinta y captura interacción. No es fuente de verdad. **Herramientas de área** (MC13I desarchivar, MC13N regresar ignoradas) viven en menú discreto `#aa-lists-area-tools` (MC13N-2), no como `data-tasks-action` de item/lista visible. **Listas colapsables (MC13L):** solo presentación en `executableListRenderer` (`<details>`/`<summary>`); sin persistencia ni cambio de feed/policies.

## Veda de los Models (regla operativa)

A partir de ahora, los archivos de `includes/models/` están **en
congelación** para añadidos:

- ❌ **NO se añaden métodos nuevos** a `AssignmentsModel` ni a
  `ReservationsModel`.
- ✅ **Métodos SQL nuevos** van a `includes/repositories/AssignmentsRepository.php`
  o `includes/repositories/ReservationsRepository.php`.
- ✅ **Reglas de negocio nuevas** (incluso si "necesitan datos") van a
  `includes/domain/{contexto}/`. El Domain Service llama al Repository
  para los datos crudos.

Los métodos existentes en los Models siguen funcionando y son llamables
también vía el Repository (por herencia), así que **no hay urgencia de
migrar consumidores**. La migración es por contagio:

> *Cuando toques un consumidor por otra razón (feature nueva, fix de bug,
> refactor), aprovecha y cambia su llamada de `AssignmentsModel::foo()`
> a `AssignmentsRepository::foo()`. Si el método mezcla SQL + reglas,
> ese es el momento de descomponerlo.*

Cuando ningún consumidor importe ya los archivos de `includes/models/`,
podremos vaciarlos y finalmente eliminarlos.

### Métodos del Model con deuda explícita (a descomponer al tocarlos)

Métodos identificados que mezclan SQL + semántica de negocio. Cuando se
toquen por una feature, fix o refactor, se parten en dos: la parte SQL
queda en el Repository correspondiente, la regla "qué cuenta como X" se
mueve a un Domain Service en `includes/domain/{contexto}/`.

#### En `AssignmentsModel`

- `AssignmentsModel::get_busy_ranges_by_assignment_ids()` — define qué
  ventanas se consideran "ocupadas" para una asignación.
- `AssignmentsModel::create_assignment()` — embebe la regla "una zona no
  puede tener dos asignaciones activas que se traslapen" y devuelve
  `['error' => 'La zona seleccionada ya tiene una asignación en ese horario']`
  como mensaje de negocio.
- `AssignmentsModel::is_service_public_calendar()` — regla de
  compatibilidad hacia atrás: `true` si la columna `public_calendar` no
  existe (legacy) o si existe y vale `1`.
- `AssignmentsModel::delete_service()` — semántica "ocultar servicio =
  `is_hidden=1` + `active=0`" embebida en el UPDATE.
- `AssignmentsModel::delete_assignment()` — semántica "ocultar asignación =
  `status='inactive'` + `is_hidden=1`" embebida en el UPDATE.
- `AssignmentsModel::get_service_areas($only_active = true)` — embebe la regla "activa = active=1" en SQL. Al descomponer: SQL parametrizado → Repository, definición de "activa" → Domain.

#### En `ReservationsModel`

- `ReservationsModel::has_confirmed_staff_overlap()` — regla "qué cuenta
  como overlap entre reservas confirmadas del mismo staff".
- `ReservationsModel::get_pending_conflicts_for_staff_overlap()` — regla
  de "conflicto" entre pendientes vs confirmadas del mismo staff.
- `ReservationsModel::get_pending_conflicts_overlapping()` — regla de
  "conflicto" entre pendientes que se traslapan en tiempo.
- `ReservationsModel::get_internal_busy_slots()` — define qué cuenta como
  "fixed busy slot" (`estado='confirmed'` + `assignment_id IS NULL` +
  `not finished`).
- `ReservationsModel::get_pending_conflicts()` — `@deprecated`. Define
  "conflicto" como coincidencia exacta de `fecha`. Cuando se elimine al
  consumidor, retirar también el método.
- `ReservationsModel::get_confirmed_overlap_in_area()` — la regla de
  overlap (`r.fecha < end AND DATE_ADD(...) > start`) y el filtro
  `estado='confirmed'` viven embebidos en el SQL; al descomponer, la
  parte SQL queda en `ReservationsRepository` y la decisión "qué cuenta
  como ocupación" se ancla en `AA_Area_Availability_Service` (que ya la
  consume).

> Esta lista no es exhaustiva. Si al tocar un consumidor descubres otro
> método del Model que mezcle SQL + reglas, añádelo aquí en el momento.
